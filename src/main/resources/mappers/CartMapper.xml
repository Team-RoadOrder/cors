<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.gmpark.cors.mappers.CartMapper">
    <insert id="insertCart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `cors`.`carts` (`user_email`, `item_id`, `size`, `quantity`, `created_at`)
        VALUES (#{userEmail}, #{itemId}, #{size}, #{quantity}, NOW())
    </insert>

    <select id="selectCartByUserEmail" resultType="dev.gmpark.cors.vos.CartVo">
        SELECT t1.`id` AS `id`,
               t1.`user_email` AS `userEmail`,
               t1.`item_id` AS `itemId`,
               t1.`size` AS `size`,
               t1.`quantity` AS `quantity`,
               t1.`created_at` AS `createdAt`,
               si.`shop_id` AS `shopId`,
               si.`item_name` AS `itemName`,
               si.`color` AS `itemColor`,
               si.`price` AS `itemPrice`,
               (SELECT img.`image_path` FROM `cors`.`shop_item_images` img WHERE img.`product_id` = si.`id` LIMIT 1) AS `itemImage`
        FROM `cors`.`carts` t1
        JOIN `cors`.`shop_items` si ON t1.`item_id` = si.`id`
        WHERE t1.`user_email` = #{userEmail}
        ORDER BY t1.`created_at` DESC
    </select>

    <delete id="deleteCartItem">
        DELETE
        FROM `cors`.`carts`
        WHERE `id` = #{id}
    </delete>

    <delete id="deleteCartItems">
        DELETE
        FROM `cors`.`carts`
        WHERE `id` IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectCartItemsByIds" resultType="dev.gmpark.cors.vos.CartVo">
        SELECT t1.`id` AS `id`,
               t1.`user_email` AS `userEmail`,
               t1.`item_id` AS `itemId`,
               t1.`size` AS `size`,
               t1.`quantity` AS `quantity`,
               t1.`created_at` AS `createdAt`,
               si.`shop_id` AS `shopId`,
               si.`item_name` AS `itemName`,
               si.`color` AS `itemColor`,
               si.`price` AS `itemPrice`,
               (SELECT img.`image_path` FROM `cors`.`shop_item_images` img WHERE img.`product_id` = si.`id` LIMIT 1) AS `itemImage`
        FROM `cors`.`carts` t1
        JOIN `cors`.`shop_items` si ON t1.`item_id` = si.`id`
        WHERE t1.`id` IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectCartCount" resultType="int">
        SELECT COUNT(*) FROM `cors`.`carts` WHERE `user_email` = #{userEmail}
    </select>

    <!-- 중복 확인용 -->
    <select id="selectCartItem" resultType="dev.gmpark.cors.entities.CartEntity">
        SELECT * FROM `cors`.`carts`
        WHERE `user_email` = #{userEmail} AND `item_id` = #{itemId} AND `size` = #{size}
        LIMIT 1
    </select>

    <!-- 수량 업데이트용 (기존 수량에 더하기) -->
    <update id="updateCartQuantity">
        UPDATE `cors`.`carts`
        SET `quantity` = `quantity` + #{quantity}
        WHERE `id` = #{id}
    </update>

    <!-- 수량 업데이트용 (특정 값으로 변경) -->
    <update id="updateCartQuantityById">
        UPDATE `cors`.`carts`
        SET `quantity` = #{quantity}
        WHERE `id` = #{id}
    </update>
</mapper>
